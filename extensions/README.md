# Homebrew Extensions

Each subfolder is a pack with a `manifest.json`. The loader reads manifests,
orders them by priority, and can apply overrides in higher-priority packs.

Start by copying `example_pack` and updating the manifest fields.
